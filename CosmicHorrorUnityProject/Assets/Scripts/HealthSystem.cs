/*using UnityEngine;
using UnityEngine.UI;
public class HealthSystem : MonoBehaviour
{
    public Sprite full, loss;
    //image () add image here
    private void Awake()
    {
        //  This is where I'd put an image, if I had one
    }
    public void SetHP(HPcurrent status)
    {
        switch (status)
        {
            case HeartStatus.Loss:
                //heartImage.sprite = emptyhp;
                break;
            case HeartStatus.Full:
                //heartImage.sprite = fullhp;
                break;
        }
    }
}
public enum HPcurrent
{
    Loss = 0,
    Full = 1
} */